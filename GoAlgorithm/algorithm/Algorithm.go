package algorithm

//stations map[station]StationWithJourneys[]ticket.Ticket

//JavaCode

/*public Map<Integer, List<Train>> findIndirectTrains(List<Train> allTrains, Station start, Station end) {
Map<Integer, List<Train>> indirectTrainsMap = new TreeMap<>();
int count = 0;

List<Train> trainsWithStartStation = allTrains.stream()
.filter(a -> a.getStationList().contains(start))
.collect(Collectors.toList()); // trains that contains start station
List<Train> trainsWithEndStation = allTrains.stream()
.filter(a -> a.getStationList().contains(end))
.collect(Collectors.toList()); // trains that contains end station*/

//func findConnectionJourneys(startStation string, endStation string) {

//journeyGroups:=list.New()
//startStationJourneys=stations.[startStation].getJourneys()
//endStationJourneys=stations.[startStation].getJourneys()
//}

//func FindIndirectTrains(allTrains []train.Train, stationStart station.Station, stationEnd station.Station) map[string][]train.Train {
//	IndirectTrainsMap = make(map[string][]train.Train)
//	count := 0
//}

func main() {

}
